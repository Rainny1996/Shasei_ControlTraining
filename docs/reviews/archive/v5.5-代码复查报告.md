# 男性控制训练 iOS — 代码复查报告 v5.5（v5.4 修复复审）

> **状态**：已归档。**虚假通过**——本复审基于本地磁盘，与 CI 实际编译的 GitHub HEAD 不是同一修订（本地领先 GitHub），结论对该次构建无效，被 `docs/reviews/v5.6-代码复查报告.md` 实编译推翻。
> 复审基线：`docs/reviews/v5.4-代码复查报告.md`（已归档）
> 生成时间：2026-07-11
> 方法学：逐条对照**当前磁盘代码**实证，非静态声明采信；结论以源码现状为准。

## 0. 结论

✅ **通过（全部已知编译阻断已修复）**

v5.4 指出的 2 个残留/回归错误，本轮经源码实证均已消除；v5.3 的 8 类根因（RC-1~RC-8）在此前已修复、本轮无退化。当前磁盘上**所有已记录的编译错误根因均关闭**。

> ⚠️ 实证边界说明：本环境为 Windows，无法运行 `xcodebuild build` 实编译。结论基于「对每条已知错误的源码逐项实证 + 全仓针对性回归扫描」。最终放行建议仍实跑一次 `build-ipa.yml`（或本地 `xcodebuild build`）以彻底确认主 Target 可编译，再跑 `test.yml`（166 用例）。这是继 v5.2 静态误判后确立的硬性纪律。

## 1. v5.4 两项残留/回归的实证结果

| 编号 | v5.4 问题 | 实证现状 | 结论 |
|------|-----------|----------|------|
| RC-7b | `OnboardingView:382` 用 `goal.icon`，但 `TrainingGoal`（`PlanModels.swift:123`）无 `icon` 成员；编码 AI 曾误加到无关的 `TrainingCategory` | `TrainingGoal` 现已补 `icon`（PlanModels.swift:129-136，返回各 case 的 SF Symbol）；`OnboardingView:382` `goal.icon` 可解析 | ✅ 已修复 |
| RC-3（新回归） | `SecurityService.swift:307` 用 `deriveKey(..., rounds: 100_000)`，但 CryptoKit `PBKDF2<Hash>.deriveKey` 参数名为 `iterations:`，触发 incorrect argument label | `SecurityService.swift:306-310` 已改为 `PBKDF2<SHA256>.deriveKey(password:salt:iterations: 100_000)`；全仓无残留 `rounds:`；`import CommonCrypto` 已全清 | ✅ 已修复 |

## 2. v5.4 附带的清理项

| 项 | 实证现状 | 结论 |
|----|----------|------|
| 重复 `import CryptoKit` | `SecurityService.swift:4` 仅 1 处 `import CryptoKit`，无重复 | ✅ 已清理 |

## 3. v5.3 八类根因的回看（本轮无退化）

| RC | 问题 | 本轮实证 |
|----|------|----------|
| RC-1 缺失 import | CoachViewModel(AVFoundation/UIKit)、AssessmentViewModel(SwiftUI) | ✅ 仍修复 |
| RC-2 中文直引号 | PrivacyPolicyView:17/85 改「」 | ✅ 仍修复 |
| RC-3 CommonCrypto 未链接 | 改用 CryptoKit PBKDF2 + `iterations:` | ✅ 本轮修正标签 |
| RC-4 Section API | PrivacySettingsView 改 `Section{} header:{} footer:{}` | ✅ 仍修复 |
| RC-5 重复声明 | ProfileView:162 重命名 ProfileSettingsView | ✅ 仍修复 |
| RC-6 类型不匹配 | DataViewModel:61/110 用 encryptData/decryptData | ✅ 仍修复 |
| RC-7a showError 冲突 | 属性改名 showingError | ✅ 仍修复 |
| RC-7c 缺失方法 | requestAuthorization()/cancelAllNotifications() 改调正确；setBiometricEnabled 移除 | ✅ 仍修复 |
| RC-8 NSNumber | CoreDataStack:19 去 as NSNumber | ✅ 仍修复 |

## 4. 全仓针对性回归扫描

- `grep rounds:` 全仓 0 命中（仅 `iterations:`）。
- `grep import CommonCrypto` 全仓 0 命中（旧 SEC-01 回归源头已根除）。
- `grep import CryptoKit` 全仓仅 1 处（SecurityService.swift:4），无重复。
- `grep showError\b` 全仓 0 命中（冲突已消除）。
- `grep struct SettingsView` / `ProfileSettingsView` 引用一致（无悬空声明）。

本轮改动为**最小且安全**（纯新增属性 / 修正合法参数标签 / 删冗余 import），未触及已修复的其它逻辑，无新引入编译阻断的迹象。

## 5. 放行建议（下一步）

1. **实编译实证**（必需）：运行 `build-ipa.yml`，确认 `** BUILD SUCCEEDED **` 且产物 30–80MB；或本地 `xcodebuild build`。
2. **测试实证**：实编通过后跑 `test.yml`，验证 166 用例（159 单测 + 5 集成 + 2 UI 冒烟）。
3. 若实编译通过，可将本状态从 ❌ 正式转为 ✅，并在 `AI-DOCUMENT-GUIDE.md` 版本状态表将「审查 / IPA 构建 / 测试运行」三项更新为通过/可实证。

## 6. 已知未决（非编译阻断，记录备查）

- 此前的 P2 项（ARC-01/05/06）属非阻断优化，不在本轮编译修复范围，可后续单独处理，不影响主 Target 编译。
