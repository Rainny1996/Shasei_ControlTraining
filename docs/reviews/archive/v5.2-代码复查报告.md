# v5.2 代码复查报告（基于 v5.1 复查报告的修复复核）

> 复核日期：2026-07-11
> 复核对象：实现方根据 `archive/v5.1-代码复查报告.md` 所做的修复
> 复核方式：静态代码复查（对照源码逐条实证；当前环境无 macOS/Xcode，未运行 `xcodebuild`，但 5 项 P0 编译阻断均已消除，主 Target 应可编译）

## 一、总体结论

**✅ 通过 —— 主 Target 编译阻断已全部消除。**

实现方修复了 v5.1 遗留的唯一 P0（BUG-CT-11），并顺势完成了全部 P1（SEC-01/02/03）与 3 项 P2（ARC-03/04/07）。当前 5 项 P0、3 项 P1、3 项 P2 均已闭合；仅剩 ARC-01/05/06 三项 P2（既有架构级、非编译阻断），建议排期处理但不阻塞构建与测试。

> ⚠️ 说明：本结论为静态源码实证。建议在 macOS 环境以 `xcodebuild build` 实证主 Target 可编译，并运行 `.github/workflows/test.yml` 验证 v4.0 测试体系（166 用例）。

## 二、逐项复核结果

| 原编号 | 问题 | 复核结论 | 证据 |
|--------|------|----------|------|
| BUG-CT-08 | CoachViewModel 重复 deinit | ✅ 已修复 | 仅 1 个 `deinit`（`CoachViewModel.swift:140`） |
| BUG-CT-09 | isPartial 未入模型 | ✅ 已修复 | 模型第 37 行含 `isPartial`；`DataViewModel.swift:139` 写入 `cd.isPartial` |
| BUG-CT-10 | clearAll() 不存在 | ✅ 已修复 | `DataViewModel.swift:179` 调用 `keychainService.clearAllSensitiveData()` |
| BUG-CT-11 | status 引用 | ✅ 已修复 | `DataViewModel.swift:208-211` 移除 `status`，仅 `id/date/checkInTime`；全仓已无 `$0.status`/`cd.status`/`CheckInEntry(...status)` 残留；`CDCheckInRecord` 模型（60-66 行）本就无 `status`，一致 |
| BUG-CT-12 | methodId 类型不匹配 | ✅ 已修复 | `RecordEntry.methodId` 声明为 `UUID`（`DataViewModel.swift:232`），`CDTrainingRecord.methodId` 同为 UUID（模型 30 行） |
| SEC-01 | 4 位 PIN + 单次 SHA256 | ✅ 已修复 | `SecurityService.hashPassword`（304-321 行）改用 PBKDF2-SHA256，100,000 次迭代 |
| SEC-02 | memset 对 Data 无效 | ✅ 已修复 | `secureDelete`（286-291 行）已移除无效 `memset`，改为注释说明 CoW 限制 |
| SEC-03 | 导入校验逻辑倒置 | ✅ 已修复 | `DataViewModel.swift:114` 为 `!exports.trainingRecords.isEmpty || !exports.checkInRecords.isEmpty` |
| ARC-01 | 字段级 AES 未落地 | ❌ 未修 | `EncryptedField` 仅定义于 `CryptoService.swift:157`，未接入 Core Data 实体（既有架构问题，非阻断） |
| ARC-03 | deleteAllUserData 竞态 | ✅ 已修复 | `CoreDataStack.deleteAllUserData`（75-95 行）在 `performBackgroundTask` 批删+save 完成后的 `DispatchQueue.main.async` 中才 `viewContext.reset()`，竞态消除 |
| ARC-04 | 呼吸维度过滤失效 | ✅ 已修复 | `AnalysisService.calculateBreathCoordination`（118-130 行）移除 `records.filter { $0.methodId != UUID() }` 恒真过滤 |
| ARC-05 | 导出丢弃计划/档案/复盘 | ❌ 未修 | `collectAllData`（194-215 行）仍仅导出 `trainingRecords` 与 `checkInRecords`；`DataExport`（225-228 行）无计划/档案/复盘字段（既有设计不足，非阻断） |
| ARC-06 | 读写上下文不一致 | ❌ 未修 | 既有架构问题；`DataViewModel` 导出/导入均统一用 `newBackgroundContext()`，未发现新增不一致（非阻断） |
| ARC-07 | 双重生物识别 | ✅ 已修复 | `SecurityService.configureProtection`（232-236 行）已清空冗余前台监听；全仓 `willEnterForeground` 仅 `AppDelegate:23` 一处触发 |

## 三、放行决策

| 检查项 | 状态 |
|--------|------|
| 主 Target 可编译 | ✅ P0 全消除（静态实证；建议 `xcodebuild build` 最终确认） |
| 测试 Target 可编译/运行 | ⏳ 待实证（依赖主 Target，现应可编译，可跑 `test.yml`） |
| IPA 可产出 | ⏳ 待实证（跑 `build-ipa.yml` 确认产物为 30-80MB） |

**整体放行：✅ 通过（编译阻断已消除）。** 剩余 ARC-01/05/06 为 P2 非阻断项，建议排期。

## 四、后续建议（按优先级）

1. **P2（建议排期）**：
   - ARC-01：将 `EncryptedField` 落地到 Core Data 实体，或修正设计文档声明（当前声明与实现不符）。
   - ARC-05：`collectAllData`/`DataExport` 补全计划（`CDTrainingPlan`/`CDPlanItem`）、能力档案（`CDAbilityProfile`）、复盘（`CDReviewNote`）的导出与导入，避免备份残缺。
   - ARC-06：统一读写上下文策略（如需）。
2. **实证**：在 macOS 环境运行 `xcodebuild build` 与 `test.yml`，确认主 Target 可编译且 166 用例通过，并以 `build-ipa.yml` 验证 IPA 产物大小。
