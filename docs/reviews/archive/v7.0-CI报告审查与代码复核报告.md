# v7.0 - CI 报告审查与代码复核报告

- **审查对象**: `docs/github log/构建报告-2026-07-12-BuildIPA-25.md`(Build IPA #25)、`docs/github log/测试报告-2026-07-12-Test-11.md`(Test #11)
- **声称提交**: `97ca7c2`(完整 SHA: `97ca7c254f86fc02b209addd0bd928fed28fcdcd`)
- **当前工作区 HEAD**: `97ca7c2`(与报告声称一致),且**代码文件无任何未提交改动**(仅 docs 与本次产出)
- **审查方式**: 源码级静态复核(Windows 环境无法编译 Swift);并解压核验 Test #11 真实 artifact `docs/github log/test/test-log.zip → test.log`
- **结论**: ⚠️ **两份 CI 报告均不适用于驱动代码修复**——其描述的全部编译错误在当前 `97ca7c2` 代码中**已不存在**。报告存在严重误植/过时,疑似运维 AI 在 GitHub API 限速、未能取到真实 artifact 的情况下,将**更早版本**的错误归因到了 `97ca7c2`。

---

## 一、报告声称的失败(摘要)

| 报告 | 声称错误 |
|------|----------|
| Build IPA #25 (Release) | ① `SettingsViewModel.swift:63` 调用 `NotificationService.shared.requestAuthAndScheduleDefaults()` 不存在;② `:65` `cancelAll()` 不存在;③ `:72` `SecurityService.shared.setBiometricEnabled(enabled)` 不存在 |
| Test #11 (Debug) | ① `CoreDataStack.swift:6` `expected declaration`/`expected '}' in class`;② `DataController has no member 'shared'`(×8);③ `invalid redeclaration of 'SettingsView'`;④ `ambiguous use of 'init()'`;⑤ "6 个系统框架缺失"(UserNotifications/Security/LocalAuthentication/CryptoKit/Charts/AVFoundation) |

---

## 二、代码复核:报告错误与当前代码逐一对照

### Build IPA #25 的三处错误 —— 全部不存在

| 报告声称 | 当前 `SettingsViewModel.swift` 实际代码 | 判定 |
|----------|------------------------------------------|------|
| `:63` `requestAuthAndScheduleDefaults()` | `:63` `NotificationService.shared.requestAuthorization()` —— `NotificationService.swift:36` 已定义 `func requestAuthorization()` | ❌ 误报 |
| `:65` `cancelAll()` | `:65` `NotificationService.shared.cancelAllNotifications()` —— `NotificationService.swift:277` 已定义 `func cancelAllNotifications()` | ❌ 误报 |
| `:72` `setBiometricEnabled(enabled)` | `:69-73` `toggleBiometric` 内为 `UserDefaults.standard.set(enabled, forKey: "useFaceID")`,**根本未调用 `SecurityService`**;且 `SecurityService.swift` 当前无 `setBiometricEnabled` 方法 | ❌ 误报 |

> 全仓搜索 `requestAuthAndScheduleDefaults` / `cancelAll()` / `setBiometricEnabled` —— **0 命中**。报告建议"补充这 3 个方法"若被执行,会凭空制造不存在的 API 调用,污染已正确的代码。

### Test #11 的编译错误 —— 全部不存在(且真实 artifact 暴露版本错位)

| 报告声称 | 当前代码实际 | 判定 |
|----------|--------------|------|
| `CoreDataStack.swift:6` `expected declaration` | `:5-6` `class DataController: ObservableObject { static let shared = DataController() }` 完全合法 | ❌ 误报 |
| `DataController has no member 'shared'`(×8) | `CoreDataStack.swift:6` `static let shared` 存在,8 处 `DataController.shared` 引用合法 | ❌ 误报(系上面语法错的级联) |
| `invalid redeclaration of 'SettingsView'` | `SettingsView.swift:4` `struct SettingsView`;`ProfileView.swift:162` 已改名为 `struct ProfileSettingsView`(无重名) | ❌ 误报 |
| `ambiguous use of 'init()'` | 仅 `SettingsView` 一个声明,`#Preview` 中 `SettingsView()` 无歧义 | ❌ 误报 |

**决定性证据(`test.log` 真实 artifact)**:
```
CoreDataStack.swift:6:5: error: expected declaration
    static let shared = DataController()            ← 构建时此行报 "expected declaration"
ProfileView.swift:162:8: note: found this candidate
struct SettingsView: View {                          ← 构建时此行仍是 struct SettingsView(重声明)
```
而当前工作区 `CoreDataStack.swift:6` 合法、`ProfileView.swift:162` 已是 `struct ProfileSettingsView`。**同一提交不可能同时具备"已修复"与"损坏"两种状态** → 该 `test.log` 对应的代码版本**早于 `97ca7c2`**,被运维 AI 错误标记为 `97ca7c2` 的产物。

### Test #11 "6 个框架缺失" —— 系编造,真实日志中不存在

在真实 `test.log` 中搜索 `No such file or directory` 及 `UserNotifications.framework`/`Security.framework`/`LocalAuthentication`/`CryptoKit.framework`/`Charts.framework`/`AVFoundation.framework` —— **0 命中**。该小节并未出现在真实 artifact 中,属报告误植。

---

## 三、当前 `97ca7c2` 代码静态一致性核查(供编码 AI 放心)

为排除"报告虽过时、但当前代码仍有别的错"的可能,对 app + tests 两个 target 做了全量符号核验:

**App target**
- `CoreDataStack.swift`:`DataController.shared` ✅
- `SettingsView.swift` / `ProfileView.swift`:`SettingsView` 唯一声明,`ProfileSettingsView` 已重命名 ✅
- `SettingsViewModel.swift`:调用的 `requestAuthorization` / `cancelAllNotifications` / `UserDefaults` 均存在 ✅
- `SecurityService.swift`:`isSecurityEnabled()` 存在,无 `setBiometricEnabled` 且无人调用 ✅
- `NotificationService.swift`:`requestAuthorization(:36)` / `cancelAllNotifications(:277)` 存在 ✅
- `PlanEditView.swift`:`#Preview` 已按 v6.0 建议修复(`let vm: PlanViewModel = {...}()`),全仓 26 个 `#Preview` 无裸赋值 ✅
- `PlanRepository.swift`:97ca7c2 删除的 `addPlanItem/updatePlanItem/removePlanItem/recomputeProgress` 为死代码,全仓 0 调用 ✅;`saveTrainingPlan/fetchActivePlan/updatePlanItems` 保留 ✅

**Tests target(需求10/11)**
- `PlanViewModel`:`editingDraft`(:28)、`beginPlanEditing`(:245)、`editItemMethod`(:257)、`editItemDuration`(:271)、`editItemDate`(:285)、`addItem`(:299)、`removeItem`(:310)、`savePlanEdits`(:324 返回 `[PlanEditValidationError]`)、`createPlanFromTemplate`(:132)、`adjustPlanIfNeeded`(:160)、`regeneratePlan`(:139)、`loadPlan`(:95) 全部存在 ✅
- `PlanService`:`planTemplates()`(:368)、`saveUserTemplate()`(:529) 存在,且 `saveUserTemplate` 正确转发到 `PlanRepository.saveUserTemplate` ✅
- `SpyPlanRepository` 覆写的 `updatePlanItems` 未被删除 ✅

**结论**:当前 `97ca7c2` 源码在其描述范围内**内部一致、符号齐备**,报告所列错误均已被此前的修复提交消除。

---

## 四、真实根因与处理建议(给运维 AI / 编码 AI)

### 根因
Build IPA #25 与 Test #11 所报告的失败,根因是**早于 `97ca7c2` 的代码缺陷**:
- `CoreDataStack.swift` 类体语法损坏(导致 `DataController.shared` 全工程无法解析,级联 8 处错误);
- `SettingsView` 被重复声明(旧 `ProfileView` 内亦为 `SettingsView`);
- `SettingsViewModel` 曾调用 3 个当时不存在的通知/安全 API。

这些缺陷**已在 `97ca7c2`(及其前身)被修复**,运维 AI 在无法取到真实 artifact 的情况下,将旧日志/旧错误描述套用到了 `97ca7c2`,造成"报告与代码对不上"。

### 给编码 AI 的明确指引(请勿盲目套用报告建议)
1. 🚫 **不要**按 Build IPA #25 的建议去"新增 `requestAuthAndScheduleDefaults` / `cancelAll` / `setBiometricEnabled`"——这些方法在当前代码中并非缺失,真实 API 是 `requestAuthorization` / `cancelAllNotifications` / `UserDefaults.standard.set(...)`。新增会引入无主调用、破坏已正确逻辑。
2. 🚫 **不要**去"修复 `CoreDataStack` 语法 / 去掉 `SettingsView` 重声明"——当前代码已合法,改动反而可能引入 `{}` 不匹配。
3. ✅ **正确动作**:重新触发针对当前 `97ca7c2` 的 CI(`xcodebuild build` + `xcodebuild test`),获取**真实**最新日志;若在 macOS 上确认编译通过,则无需任何代码改动;若重跑后出现**新**的具体错误,再针对该错误单独修复,并回填本报告。
4. ✅ 建议运维 AI 修正报告生成流程:在 GitHub API 限速时标记"artifact 未取",而非回退到旧错误文本,避免误导后续修复。

### 验证限制
本次为 **Windows 静态/源码级复核,未实际编译 Swift**。权威结论需以 macOS 上针对当前 `97ca7c2` 重跑 `xcodebuild` 为准;但所有被报告点名的错误均已确认在源码中不存在,静态一致性核查全部通过。

---

## 五、随附产物
- 真实 artifact 已解压于 `docs/github log/test/extracted/test.log`(作为本报告"版本错位"论断的一手证据)。
- 建议:待 CI 在 `97ca7c2` 重跑通过后,可将 Build IPA #25 / Test #11 两份过时报告移入 `docs/github log/` 的归档区,并据此更新 `docs/github log/README.md`(原标记"待处理/限速")。
